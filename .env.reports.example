# Reports-only production configuration. Do not copy JWT or Monitor Agent secrets here.
RUSTZEN_ENV=production
RUSTZEN_RUNTIME_ROOT=/opt/rz
RUSTZEN_TIMEZONE=UTC
RUSTZEN_REPORTS_SQLITE_PATH=/opt/rz/data/reports/db/reports.db
RUSTZEN_IPC_TOKEN=replace-me
RUSTZEN_REPORTS_CREDENTIAL_KEY=replace-me
