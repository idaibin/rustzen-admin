[workspace]
members = [
    "apps/admin",
    "apps/cli",
    "apps/insights",
    "apps/monitor",
    "apps/reports",
    "crates/auth",
    "crates/config",
    "crates/ipc",
    "crates/runtime",
    "crates/storage",
]
resolver = "2"

[workspace.package]
version = "0.5.1"
license = "Apache-2.0"

[profile.dev]
# Keep useful backtraces without retaining full variable-level debug data for
# every workspace dependency. The test profile inherits this setting.
debug = "limited"

[profile.release]
opt-level = "z"
lto = "thin"
codegen-units = 1
panic = "abort"
strip = true
